# 更新记录

## 2026-09-21 v1.0（正式版）

**前面那些 v0.2 ~ v1.15.2 都是开发过程中的测试版，从这一版起重新从 v1.0 算。**

- **令牌彻底写进程序里**：上传曲库不再需要任何填写 —— 界面上原来那一行
  （输入框 / 保存 / 清除 / 「去哪儿拿令牌」的说明）整块**删掉了**。
  令牌就是 `github_token_local.py` 里的 `TOKEN`（这个文件不进 git），
  `library.get_token()` 直接返回它。打开「上传 / 整理曲库…」就能传。
  想换令牌：改那个文件里的 `TOKEN` 重新打包。
- **上传界面的提示改成好好说话**：不再是一排红字警告，换成一句
  「这个曲库是公开的，大家一起用 🙂 传上去的曲子所有人都能看见、能下载 ——
  点『上传』就等于同意把它分享出去。只想留着自己听的，就别往这儿传。」
  配色也从警示红换成了主色的浅底（`QLabel#notice`）。
- **桌面上用编辑器时主窗口不置顶**：停在「简谱编辑器」那一页、人又在桌面上时，
  主窗口撤掉「总在最前」—— 编辑器是对着别的东西改谱用的（查资料、看原谱、开播放器），
  一直压在最上面就只能靠最小化才能切窗口，太别扭。切回「演奏」页、或者人在游戏里，
  置顶立刻恢复（游戏里浮层必须贴着游戏）。判断见 `main._editing_on_desktop()`，
  换页 / 进出游戏时由 `_sync_overlay_topmost()` 补状态；只动 Win32 的 topmost 位，
  不 `setWindowFlags`，所以不会闪一下或者丢焦点。（「游戏内覆盖」的悬停说明里也写了这一条。）
- **版本号回到 v1.0**：安装包是 `AutoPlay 完全版 安装程序 v1.0.exe` /
  `AutoPlay 精简版 安装程序 v1.0.exe`。
- ⚠ 一句实话：令牌写死了就跟着安装包走，谁把包里的字节码挖出来谁就拿到了它。
  自己用没问题，**别把带令牌的包发给别人** —— 要发就先删 `github_token_local.py` 再打包。
  （下面那个「上传中转」就是专门解这个死结的。）

### 同期：上传中转（让安装包里不带令牌，用户还是不用填）

- **新增上传中转**：`relay.py` / `relay_source.py` / `set_relay_url.py` /
  `设置上传中转并重新打包.bat`，以及 `中转上传\`（Cloudflare Worker
  `worker.js` + `部署说明.md`）。配了中转之后，上传走

  ```text
  程序  →  你自己的 Worker（令牌在它身上）  →  GitHub 仓库（顺手重排 library.json）
  ```

  安装包里**一行令牌都没有**，但用户依旧什么都不用填、打开就能传；
  想换令牌、想停上传，在 Cloudflare 上改一下就行，**用户无感、不用重新打包**。
  搭建费用为零（Workers 免费额度），十分钟完事。
- `set_relay_url.py <地址> --strip-token`：写中转地址的同时**清掉
  `github_token_local.py` 里的令牌** —— 这样打出来的包可以直接公开发布。
  没配中转时行为不变：走内置令牌，都没有就只提示一句。
- 上传窗口里多一行「上传通道：…」，写清楚这次走哪条路
  （中转 / 内置令牌），看一眼就知道为什么不用填令牌。
- 中转自带一道防滥用：只收 midi（看文件头 `MThd`）、限单文件大小、
  同一 IP 每小时限次数、可选的 `UPLOAD_KEY` 口令，文件名会被收拾干净
  （拿 `../../etc/a:b?.mid` 试过，只能落在仓库根目录）。
- **修一个老 bug**：传上去的**新**曲子，填的标题 / 艺术家以前是白填的
  （重排索引时只从旧索引里取标题，新曲子在旧索引里当然没有），
  现在会真写进 `library.json`（`library.refresh_index(..., meta=...)`
  和 Worker 的 `buildIndex(..., meta)`）。
- **修**：中转说的话（比如 404 时的「能用的只有 /health、/upload、/reindex」）
  现在会原样显示给用户，不再被换成一句笼统的「地址是不是写错了？」。
- 中转客户端把**文件名放在单独的字段里**，文件本身用纯 ascii 假名 ——
  中文文件名的 RFC 2231 编码坑不在客户端碰。
- **中转口令（`UPLOAD_KEY`）**：在 Cloudflare 给 Worker 设一个口令，
  只有带对口令的请求才受理 —— 挡住「知道地址就往里塞东西」的人。
  客户端用 `set_relay_url.py <地址> --key <口令>` 配；
  口令写进 **`relay_key_local.py`**（不进 git，和 `github_token_local.py`
  一个待遇），而 `relay_source.py` 这个进 git 的文件只放地址。
  优先级：本机 `relay_key.txt` > `relay_key_local.py` > `relay_source.RELAY_KEY`（老写法）。
  ⚠ 口令打进 exe 也能被挖出来，它只是防滥用，不是安全措施；
  但它换不来你的 GitHub 令牌（令牌只在服务器上）。
- 配置脚本问中转的时候不再动本机配置（直接拿你给的地址 + 口令去 `GET /health`），
  避免留下一个会盖掉打包值的 `relay.txt`。

## 2026-09-20 v1.15.2

- **内置令牌**：`library.py` 会试着 import 同目录的 `github_token_local.py`，
  拿到里面的 `TOKEN` 就当默认令牌用 —— 「联网曲库 → 上传 / 整理曲库…」打开时
  那一栏已经是填好的，传曲子 / 重排索引不用再粘一遍。优先级：
  本机 `github_token_local.txt` 那份 > 内置的 > 空（点「清除」是退回内置，不是变没有，
  想换成自己的就清掉再粘新的点「保存」）。
- `github_token_local.py` **不进 git**（已经加进 `.gitignore`），所以它只在这台机器和
  自己打出来的包里存在。
- ⚠ 内置令牌会跟着安装包走：谁拿到安装包、把里面的 python 字节码挖出来，谁就拿到了这个
  令牌，能往曲库里推东西（GitHub 的 secret scanning 要是在公开仓库里扫到它还会自动作废）。
  **别把带令牌的安装包发给别人**；要发就先删掉 `github_token_local.py` 重新打包。
- `refresh_index()` 现在也会自己取默认令牌了（以前只认传进来的那个）。
- 界面上那一行说明会跟着变：内置了令牌就说「不用自己填」，没有才教你去哪儿拿。
- 两个安装包重新打成 v1.15.2。

## 2026-09-20 v1.15.1

- **仓库里只有 midi 也能用联网曲库了**：以前程序只认索引文件 `library.json`，
  仓库没放它就报「服务器回了 404」—— 现在索引这条路走不通时（没有这个文件 /
  不是 json / 拿回来是网页）会**直接问 GitHub「这个仓库里有哪些文件」**
  （`library.songs_from_repo`），把里面所有 `.mid / .midi` 列成歌单。
  所以「只往里丢 midi」的仓库开箱就能用，不用额外维护索引文件；
  有索引时还是优先读索引（标题 / 作曲家 / sha1 这些信息还得靠它）。
  这一下是匿名请求，GitHub 对匿名调用有次数限制（一小时几十次，刷新歌单够了）；
  传曲子、改索引仍然要令牌。
- 联网曲库拉不到时的提示也分了两种情况：认得出来是仓库地址就说
  「仓库里没有 midi，或者连不上 GitHub」，认不出来才让你去填地址。
- 两个安装包重新打成 v1.15.1。

## 2026-09-20 v1.15

- **主题的音符颜色也换了**：以前六套主题共用一套操作色（怕换主题之后颜色不认识）；
  现在**每套主题的六种操作色都是单独挑的**，换主题一眼能看出来。
  六种之间的区分度在每套里都保得住（等长演奏时色块很短，颜色是唯一的线索），
  也都不低于 0.7 的饱和度（`theme.py` 的 `PRESETS`，每套的 `notes` 里一行一个注释）。
  预设升级会**自动重写**：主题文件夹里多了一个 `presets.rev` 小本子记着预设是第几版
  写出去的，版本对不上就重写那六个预设文件；**自己加的主题文件从头到尾不会被动**。
- **主题 json 有说明书了**：新增 `docs/主题格式.md` —— 66 个角色各管哪儿、
  六种操作色怎么挑、预设那点色相旋转是怎么算出来的、怎么做一套自己的、
  做好怎么发给别人。README 里那一节也指过去了；六套主题的音符配色有一张
  对照图 `docs/主题音符配色.png`（主界面截图也刷成了 v1.15）。
- **编辑器那一页默认不透明**：浮层形态（游戏内唤起）下，停在「简谱编辑器」页时
  整窗不透明度给 1.0 —— 那一页信息密，半透明跟游戏画面搅在一起看着累。
  别的页照旧留一点透明（在游戏里唤起就是为了「能看见游戏、还能改点东西」）。
- **能往曲库传曲子了**：联网曲库窗口右下角「上传 / 整理曲库…」。
  - 传一首 midi：写进仓库，然后把 `library.json` **按仓库里现有的文件重新生成**一遍，
    所以索引不会写着写着和仓库对不上（标题 / 艺术家沿用旧索引里写的）；
  - 「按仓库现有文件重排索引」：不传新文件，只重排索引 —— 手往仓库里丢过文件的，
    点一下就补齐了；
  - 走 GitHub Contents API，需要一个令牌（Contents 读写），只存在本机
    `%LOCALAPPDATA%\AutoPlay\library\github_token.txt`；401 / 403 / 404 / 409 / 422
    都翻了人话；
  - ⚠ 界面上第一行红字就写着：曲库是**公开的开源仓库**，传上去的东西所有人
    都能看见、能下载。
- `library.repo_of()` 认三种曲库地址（raw / jsDelivr / github.com/blob），
  传曲子的时候不用再手填「谁的仓库、哪个分支」。
- **曲库地址已经指到你这边的仓库**（`library_source.py`，打包进 v1.15）：

  ```text
  https://raw.githubusercontent.com/xXjuanneysXx/midi-music/main/library.json
  ```

  仓库里还**没有** `library.json`，所以第一次点「联网曲库」会提示拉不到 ——
  把 `曲库索引\library.json`（已经按仓库里现有那两个 midi 算好大小和校验值了）
  提交到仓库根目录就好，或者用程序里那个「按仓库现有文件重排索引」按钮生成。
  `曲库索引\README.md` 里写了详细步骤。
- 两个安装包：`AutoPlay 完全版 安装程序 v1.15.exe` / `AutoPlay 精简版 安装程序 v1.15.exe`。

## 2026-09-20 v1.14

- **录制时按住多久就响多久**。以前监听音是一声 0.3 秒的「叮」，长按听着还是短音；
  现在每个音高合成成一段很长的**持续音**（按下先「咚」一下，之后一直保持同一音量），
  **按下开始放、松开立刻停** —— 长按终于是个长音了。临时 wav 换了新前缀
  （`monitor2-`），旧的短音文件不会被接着用（`notesound.py`）。
  这次只改了「出声」那一半：**录下来的时值本来就准**（按多久记多久），
  这一点我用 2 秒长按的用例验过了。
- **新增「音长吸附」**（录制那一行，默认 **10 毫秒**）：录完把每个时值吸到最近的格子上 ——
  按了 354 毫秒、格子上理论值是 350 毫秒，就记成 350；**空档（休止）也一起吸**，
  不然光吸音长会把节奏切碎。想原样保留就选「关」，改了几个时值会写在日志里
  （`recorder.snap_pairs`）。
- **配色主题（json）**：主界面、右上角进度浮窗、跟奏面板、编辑器**一起换**。
  - 预设 6 套：深空蓝（默认）/ 午夜紫 / 松林绿 / 琥珀暖夜 / 绯红霓虹 / 石墨灰；
  - 主题就是 json 文件（角色名 -> 颜色），第一次运行写到
    `%LOCALAPPDATA%\AutoPlay\themes\`，照着改任何一个都行；
  - 入口：标题行那个下拉框，或者**托盘图标右键 →「配色主题」**（那儿还有
    「重新载入主题文件」和「打开主题文件夹」，改完不用重启程序）；
  - 六种操作色（无操作 / 升半音 / 升调 / 升调+半音 / 降调 / 降调+半音）**不跟着主题转**：
    颜色代表「要按哪个鼠标键」，这套含义每套主题里都得对得上；
  - 「石墨灰」只把界面转成灰的，音符照样是彩色的（不然颜色就白标了）。
- **联网曲库（共享曲库）**：曲子放 GitHub 仓库里，程序读一个索引文件就知道有哪些。
  - 索引格式 `{"version":1,"songs":[{"title":…,"artist":…,"file":"songs/xxx.mid",
    "size":…,"sha1":…}]}`，`file` 写**相对路径**，所以只需要一个索引地址；
  - 程序里点「联网曲库」看歌单、点「下载并载入」下到
    `%LOCALAPPDATA%\AutoPlay\library\`，下过的曲子不用再下（有大小 / sha1 校验）；
  - **断网什么都不影响**：拉不到就显示上次拉到的歌单（缓存），
    程序自带的 `songs\`（含《鸟之诗》）照常能用；
  - 地址填在 `library_source.py` 的 `INDEX_URL`（打包进去的默认值），
    或者往 `%LOCALAPPDATA%\AutoPlay\library\source.txt` 写一行（改完重启，不用重新打包）；
  - 给了链接之后跑 **`设置联网曲库并重新打包.bat`**（或 `python -X utf8 set_library_url.py <地址>`）：
    它会先试拉一下告诉你通不通、有几首歌，再写进 `library_source.py` 并重新打包两个安装包；
  - 地址是 `raw.githubusercontent.com` 的话会自动补一个 jsDelivr 镜像再试一次
    （国内 raw 经常连不上）；中文文件名也会先转义再请求（`ordinal not in range(128)` 那个坑）。
- 两个安装包：`AutoPlay 完全版 安装程序 v1.14.exe` / `AutoPlay 精简版 安装程序 v1.14.exe`。

## 2026-09-20 v1.13

- **窗口不最大化也不会把字裁掉了**。根子有两处，都修了：
  - 样式表里的 `padding` 不算进 Qt 的「最小尺寸」，窗口一矮，布局就把按钮压到 26px 高
    （按钮上的字被上下切掉）。现在给按钮 / 下拉框 / 数字框写了 `min-height`，
    布局的最小高度就是**真实**需要的高度 —— 宁可让窗口长高一点，也不裁字；
  - 有几行本来就被撑得太宽：文件那一栏原来放的是**整条路径**（路径里没有能换行的空格，
    Qt 当它一个超长词，那一行按 660px 算），现在只显示**文件名**（完整路径进 tooltip）；
    「快捷键设置…」那个宽按钮挪到标题行当小圆钮 `⌨`（托盘右键里照旧有）；
    「凑不够就整首放慢」简称「整首放慢」；几个数字框 / 下拉框的固定宽度也收了一点。
    现在窗口拉到最小（700×560）也不会有字被切掉。
- **控制台（运行日志）默认不显示了**：以前主界面下面常驻一大块日志，又杂又占地方。
  现在默认收起，要看的在**托盘图标上右键 →「运行日志（控制台）」**（跟「快捷键设置…」
  一个路子），选择会记住；开着控制台时窗口会自动长高给它让地方，收起时窗口跟着收回去
  （控制台关着的时候，窗口的高度正好贴合内容，不再留一大块空地）。
  编辑器那一页的日志跟着**同一个开关**走：控制台关着时编辑器也只留画布，
  日志照常往缓存里写，托盘里点开就一起看到。
  顺带把那块日志里还在叫旧名字的提示统一成了「整首放慢」。
- **小修**：系统托盘不可用的机器（远程桌面 / 精简系统）启动不再报错。
- 两个安装包：`AutoPlay 完全版 安装程序 v1.13.exe` / `AutoPlay 精简版 安装程序 v1.13.exe`。

## 2026-09-20 v1.12

- **录制时那声提示音**：上一版开始「桌面在前台就把八个琴键吃掉」，按键是不往系统里送了，
  可那声「叮」还在。这一版两头一起治：
  - **吃掉的那一下现在有去处**：钩子吃掉按键之后，会把它投给一个**收键窗口**
    （消息专用窗口，不显示、不抢焦点，`recorder.py` 里的 `_sink_key`）——
    按键有个正经接收人，而不是凭空消失；
  - **吃掉的范围扩了**：除了桌面 / 任务栏，现在**输入法自己的窗口**（`Default IME` 等）、
    **explorer.exe 的所有窗口**（桌面、任务栏、文件夹）、以及**查不到前台窗口**这几种情况
    也一律吃掉（以前这几种会漏出去，正好是「叮」一声的高发区）；
  - **新增「录制时静音系统提示音」**（默认勾上）：一开始录制就把「系统提示音」那一路
    按成静音，收工自动放回去 —— 只动系统那一路，游戏、音乐、程序自己的琴键声都不碰；
    就算程序崩了没放回去，下次打开也会自动放开（`%LOCALAPPDATA%\AutoPlay\autosave\system-sound.hold`）。
- **新增「🔔 谁在响」**：录制时蹦出一声提示音却不知道谁干的？点一下，接下来 10 秒里
  只要有程序出声，就把它的进程名记到日志（`(系统提示音)` 就说明是系统那一路，
  勾上「录制时静音系统提示音」就能按住它；要是别的程序，名字也摆在那儿了）。
  这两个功能都靠 `audiowatch.py`（WASAPI 会话接口，纯 ctypes，没多拖一个依赖），
  完全版 / 精简版都有。
- **录制期间日志会记「前台是谁」**：前台窗口一换就记一行
  （`录制中前台窗口：Progman（explorer.exe，PID 85184）—— 这八个键被吃掉`），
  出问题的时候一眼就能看出按键当时是被吃掉了还是送给了游戏。
- 两个安装包：`AutoPlay 完全版 安装程序 v1.12.exe` / `AutoPlay 精简版 安装程序 v1.12.exe`。

## 2026-09-20 v1.11

- **自动保存**：录过 / 改过、但没按「保存工程」的谱面，程序每几秒悄悄留一份底
  （`%LOCALAPPDATA%\AutoPlay\autosave\`，跟「保存工程」用的是同一个 `.mproj` 格式）。
  - **正常退出**（或者存成了工程、又载入了同一份）时自己清掉；
  - 程序被**强杀 / 崩溃 / 断电** —— 没来得及清，**下次启动自动摆回编辑器**：最新的一份
    进「简谱编辑器」那一页，其余的各开一个窗口（上次开着几份就摆回来几份），并且标成
    「还没存过」（标题前面带 `*`），按「保存工程」或「导出 MIDI」之后这份底就撤了；
  - 新增 `autosave.py`；恢复在 `MainWindow._restore_autosave()`，编辑器每 2.5 秒看一眼
    （比对内容指纹，没变就不写盘，不折腾硬盘）；
  - 关程序时，**另外开着的编辑器窗口**也会问你一句「要不要存」—— 以前只问主窗口那一份，
    别的窗口里的改动会被悄没声地丢掉。
- **最大化按钮显眼了**：`□` 从灰底灰字改成蓝底描边（悬停变实心蓝），字号加粗加大，
  鼠标移上去是手型；最大化之后变成实心蓝的 `▣`，一眼能看出当前是哪个状态。
  编辑器页那个按钮跟主窗口同一套样式，两边状态始终同步（主窗口最大化之后才打开编辑器，
  也不会显示错）。
- **录制时那声系统提示音没了**：录制期间，如果前台是**桌面**（`Progman` / `WorkerW` /
  任务栏）或者**本程序自己的窗口**，`z x c v b n m ,` 这八个键会被钩子吃掉、不再传给
  系统 —— 输入法 / 桌面接住按键「叮」一声，那个提示音就是这么来的。**游戏在前台时一律
  放过**（那个音得靠游戏自己发出来），账照记，录制的内容一点不受影响。
- 两个安装包：`AutoPlay 完全版 安装程序 v1.11.exe` / `AutoPlay 精简版 安装程序 v1.11.exe`。

## 2026-09-20 v1.10

- **修好「录制时听不到声音」**：日志里那句 `Cannot play asynchronously from memory`
  是 Windows 直接弹回来的 —— `winsound` 的 `SND_MEMORY`（内存播放）**不能**和
  `SND_ASYNC`（异步）一起用，压根不是一个可以这么组合的 API。改成：每个音高只合成
  一次、写成 `%TEMP%\AutoPlayMonitor\monitor-<音高>.wav`（十几 KB），按键时用
  `SND_FILENAME | SND_ASYNC` 放文件。既不会卡住录制钩子（同步放 0.3 秒会被系统
  摘钩子），跨次运行还能接着用。
- **「录制时发声」改成纯开关**：不再判「现在在不在游戏里」，勾着就响、不勾就哑。
  游戏里的音是游戏自己放的，嫌打架就自己关掉 —— 猜来猜去反而莫名其妙。
- **双击工程文件不再起第二个程序**。以前程序开着的时候双击 `.mproj`，Windows 会
  照注册表再起一个进程，变成两个一模一样的窗口、托盘图标和全局热键互相打架。
  现在：
  - 新增 `single.py`：新进程枚举顶层窗口，靠**进程的 exe 全路径**认出「同一个程序的
    那个已在运行的实例」（不看窗口标题 —— 标题会随载入的曲子变，覆盖模式下窗口还会
    被整个重建），把要打开的文件用 `WM_COPYDATA` 塞过去，自己立刻退出。
  - 收到消息的那边**另开一个编辑器窗口**打开这个工程，不动主窗口：双击第二个工程
    就是第二个窗口，关掉窗口不影响主窗口。
  - 光找到窗口不算数，还要对面回一句「我处理了」才算交接成功（Qt 自己也会建一些
    认不出这个消息的隐藏窗口）；没人接手就照常自己开窗口，不会变成点了没反应。
  - 只有打包成 exe 之后才走这一套：源码运行时大家的 exe 都是 `python.exe`，按可执行
    文件认人一定会认错。
- 两个安装包：`AutoPlay 完全版 安装程序 v1.10.exe` / `AutoPlay 精简版 安装程序 v1.10.exe`。

## 2026-09-20 v1.9

- **编辑工程时能最大化了。** 主窗口是无边框的（没有标题栏就没有最大化按钮），所以
  「演奏」页标题行和「简谱编辑器」页右上角各放了一个 `□` 按钮，另外绑了 `F11`；
  再按一下还原。单独跑 `AutoPlayEditor.exe` 时也明确带上了系统的最小化 / 最大化按钮
  （`QWidget` 当顶层窗口默认是不带的）。切换「游戏内覆盖」形态时不会把最大化弄丢，
  切进浮层形态则会自动还原 —— 浮层就是贴着游戏的一小块，不该铺满屏幕。
- **`.mproj` 工程文件能双击直接打开了。** 以前只能先开程序、再「打开工程」。
  - 新增 `fileassoc.py`：往 `HKCU\Software\Classes` 写 `.mproj` 的 ProgID 和打开命令
    （当前用户，不需要管理员权限），改完喊一声 `SHChangeNotify` 让资源管理器跟上。
  - **安装程序**多了一个「双击 .mproj 工程文件用本程序打开（关联文件类型）」勾选框
    （只有完全版有，精简版没编辑器）；卸载时那份 `卸载.bat` 会把同样的位置删掉，
    而且只删自己写的那几项 —— 用户后来把 `.mproj` 改成别的程序打开的话不动它。
  - 主程序里也有一个同名的勾选框，可以随时开关 / 补装关联；源码运行时没有 exe 可指，
    这个勾选框就不露出来。
  - 双击工程文件进来会**直接落在编辑器那一页**并打开它；工程读不动（坏了 / 被删了）
    就留在「演奏」页，只把原因写进日志，不会把人扔进一个空编辑器里。
- **录制分「游戏内 / 游戏外」：桌面上录制会跟着按键出声，游戏里自动静音。**
  - 新增 `notesound.py`：按下琴键就合成一小段波形（几个正弦 + 快速衰减包络，像拨弦），
    用 `winsound` 的 `SND_MEMORY` 直接在**内存**里播 —— 落盘那几十毫秒是能感觉出来的。
    每个音高的波形只合一次、缓存在内存里。
  - `recorder.Recorder` 多了一个 `on_note(记号, 是否按下)` 回调（在钩子线程里调，
    回调里抛异常一律吞掉，绝不让它跑进系统的钩子回调）。
  - **游戏里不响**：判据就是那套「前台是不是全屏程序」，所以录着录着切进游戏，
    声音自己就停；回到桌面又自己响起来，不用手动关。界面上有「录制时发声」开关
    （默认开），关掉就全程静音。
- 两个安装包依旧分开打：`AutoPlay 完全版 安装程序 v1.9.exe` / `AutoPlay 精简版 安装程序 v1.9.exe`。

## 2026-09-20 v1.8

- **新增「录制」（`F10`）**：在游戏里随手弹一段，程序把你弹的记成谱。
  - 只认游戏里那八个琴键（`z x c v b n m ,`）和三个鼠标键，别的键一律放过 ——
    录制期间照样能跑能跳、热键照样管用，混不进录音里（这一点是特意做的：之前
    有人反馈演奏时按着 `Shift` 跑步会吞音，录制这边干脆从源头上不理会那些键）。
  - 记的是**按下时刻、按住多久、按下那一刻鼠标哪个键是按着的**（右键 = 升调 `A`、
    左键 = 降调 `B`、中键 = 升半音 `#`），换算出来的记号跟自动演奏时用的完全是同一套。
  - **音长是你弹的**：按多久记多久，中间的空档写成休止符 `0`；第一个音按下算 0 秒，
    按完 F10 走过去的那几秒静音不会变成谱面开头的一长串休止符。
  - **单音是硬要求**：按重叠了就把前一个截到只剩 30 毫秒（跟编辑器里那条线一致），
    截短了几个会写在日志里，提示你去编辑器里改。
  - 一次出三样：`TONIC<主音> 录音 日期 时间.txt`（谱面）、同名 `.mid`（单音，
    用 `mido` 直接写，不依赖那套 400 MB 的音频转换库），**并且直接送进「简谱编辑器」**。
    录制接完就自动成为「当前这首」，F6 立刻能回放。
  - 文件放 `%LOCALAPPDATA%\AutoPlay\recordings\`；主音跟着当前这首歌走（默认 60 / C4）。
  - 右上角浮窗多了一个「**录制中 + 录到几个音**」的状态（跟「演奏中」用不同颜色区分）。
  - 新增 `recorder.py`；菜单 / 提示 / 快捷键对话框里多一行 `F10 录制（弹一段记成谱）`。
- **修好「在游戏里点下拉框（音长 / 速度 / 跟奏位置 / 音轨）就掉回桌面」**。
  - 根因：Qt 的 `QComboBox` 下拉列表是一个**独立的顶层窗口**，弹出来时会把自己设成前台。
    实测挂上 `WS_EX_NOACTIVATE` 也没用 —— 标志确实挂上了，Qt 自己还是会再激活它一次
    （用 `SetWinEventHook` 盯前台变化抓到的：点一下 → 前台变成我们进程 → 游戏掉桌面）。
  - 改法：新增 `InlineCombo`，**列表就是主窗口里的一块子控件，全程不开第二个窗口**，
    从根上没有了「抢前台」这回事。用真鼠标注入 + 全屏假游戏验证：点开、选一行、
    点别处收起，前台全程稳住不动（31 条测试）。
  - 代价：列表收不到键盘（方向键翻不了），鼠标点选和滚轮照常 —— 浮层里本来就打不了字，够用。
- 简化版同步：录制跟编辑器绑定，**精简版里连 `F10` 那一行键位都摘掉**
  （`edition.has_recorder()`，按了只会提示用完全版）。
- 修文档错误：`jianpu.py` 里「升半音」的例子 `#B7(-4)` 写错了，应该是 `#B5(-4)`
  （`#B7` 等于 0，就是 `E1` 那个音）；补了一条全范围（-12~+25）记号 <-> 半音往返测试。
- 两个安装包依旧分开打：`AutoPlay 完全版 安装程序 v1.8.exe` / `AutoPlay 精简版 安装程序 v1.8.exe`。

## 2026-09-20 v1.7

- **修好「`Ctrl+F1` 唤起之后，一点窗口游戏就掉回桌面」** —— 这一版最重要的一条。
  - 真正的原因是：浮层上凡是「一点就要弹个新窗口」的入口都会把前台从游戏那儿抢走，
    改快捷键的对话框、系统文件框都算。现在**在游戏里一个窗口都不弹**：改快捷键的入口
    在游戏里直接变灰（**桌面上照常能点** —— 是不是「在游戏里」看的是实时状态，
    不是窗口形态；录键必须抢键盘焦点，做不到不抢），选文件一律走浮层里的列表。
  - 窗口形态换成**跟奏窗口那套配方**：`Qt::WindowDoesNotAcceptFocus`（让 Qt 自己一直
    维持 `WS_EX_NOACTIVATE`，不再只靠手动 `SetWindowLong` —— 窗口一重建那个位就会被
    Qt 抹掉，「点一下就掉回桌面」就又回来了）；`SetWindowPos` 全部补上 `SWP_NOACTIVATE`。
  - 「现在是不是在游戏里」不再只看那个勾选框：**前台压着一个盖满整屏的别人的窗口**
    （全屏游戏 / 无边框全屏）就算 —— 所以程序在桌面上开着、用户直接切进游戏，
    也认得出来，点「选择 MIDI 文件…」不会再弹系统文件框。桌面上照旧是系统文件框。
  - 新增**后台看护**（`game_watch`，250 ms 一次，窗口露着就一直在跑）：
    「在不在游戏里」一变就立刻跟界面同步（该灰的灰、该亮的亮）；浮层贴着游戏的这段时间里，
    万一前台还是被我们抢了，立刻把前台还给游戏。用户自己 `Alt+Tab` 切走的情况一概不碰。
  - **编辑器那一页要能打字**：桌面上停在「简谱编辑器」页时，窗口临时撤销「不接受焦点」
    （数字框、输入框才收得到键盘），切回「演奏」页或离开这一页立刻补回去；
    游戏里一律不撤 —— 宁可编辑器打不了字，也不能给浮层一点点抢前台的机会。
- **两个安装包**（`发布\` 下，大小明显不一样）：
  - `AutoPlay 完全版 安装程序 v1.7.exe`（205 MB，装完 402 MB）：功能一个不少。
  - `AutoPlay 精简版 安装程序 v1.7.exe`（88 MB，装完 102 MB）：只去掉「音频转 MIDI」
    和「简谱编辑器」，**跟奏、简谱生成、自动演奏、试听、内置曲库全都在**。
    装到 `%LOCALAPPDATA%\AutoPlayLite`，快捷方式名字和卸载表项也跟完全版分开，
    两个版本可以同时装在机器上互不影响。
  - 靠新增的 `edition.py` 里一个 `LITE` 开关实现：一份代码打两个包，打包精简版时
    临时把它改成 `True`（打完立刻改回来，中途出错也会在 `finally` 里写回原文）；
    `AutoPlay.spec` 读环境变量 `AUTOPLAY_LITE=1`，那一整套 basic-pitch / onnxruntime /
    librosa / numba / llvmlite / scipy / sklearn 连本地 `mp3midi` 包和 `editor.py`
    都不打进去（2792 个模块 -> 657 个）。
  - 精简版界面上不再有那两个入口，标签条也藏起来（只剩「演奏」一页）。
- `AutoPlay.spec` 里「有没有盖满整屏」的判断抽成了纯函数 `covers_monitor()`，
  可以脱开窗口单独测；`main.py` 新增 34 条窗口形态 / 前台兜底的测试。

## 2026-09-20 v1.6

- **编辑器并进主程序**：主界面左上角变成两个标签页 ——「演奏」和「简谱编辑器」。
  点「简谱编辑器…」会切过去并把当前这首自动装进去；编辑器里「导出 MIDI」之后，
  导出的文件会**自动载回主程序**接着弹，不用再手动选一遍。
  - 编辑器是懒加载的：不点进去就不建，不为它白等启动时间。
  - 嵌进去以后它不再改窗口标题、不再自己管「关窗口要不要存盘」（改由主程序退出前问），
    快捷键也换成「只有焦点在这一页时才生效」，免得在「演奏」页按空格跑去播放谱面。
  - `AutoPlayEditor.exe` 那份独立小工具照旧能打（只要编辑器、不要那 400 MB 转换时用）。
- **内置曲库 + 默认曲**：程序目录下的 `songs\` 就是曲库，安装包里默认放着《鸟之诗》；
  启动时没指定文件就自动载入曲库第一首，界面上还多了一个「内置曲库」按钮
  （永远用浮层里的文件列表打开，游戏里也不会弹窗）。往 `songs\` 里扔 .mid 就会出现在列表里。
  - 谱面写在 midi 旁边；那个目录写不进去（装在 Program Files 又没提权）时自动退到
    `%LOCALAPPDATA%\AutoPlay\scores\`，不再让整条流程卡在写文件上。
- **单文件安装程序**（`installer.py` + `make_installer.py`，`build_installer.bat` 一键出）：
  约 205 MB 的自解压 exe，拷到任何 Windows 上双击就能装，目标机器**不用装 Python**。
  - 程序本体压在安装程序自己的尾巴上（追加 zip + 16 字节「魔数 + 偏移」尾巴），
    装的时候用 `OffsetReader` 把这段内嵌 zip 当普通文件读，**不先解到临时目录**，
    省一次几百兆的复制。PyInstaller 单文件 exe 是从尾巴往回找自己数据块的，多挂一段不影响启动（实测过）。
  - 可自定义安装目录（默认 `%LOCALAPPDATA%\AutoPlay`，不需要管理员权限），
    可勾选桌面 / 开始菜单快捷方式、装完立刻运行；快捷方式用系统自带的
    PowerShell + WScript.Shell 建，不用带 pywin32。
  - 卸载：安装目录里生成 `卸载.bat`（先把自己复制到 TEMP 再删目录，不然删不掉正在跑的脚本），
    同时注册进「设置 → 应用」，`HKCU` 下写一条，不碰系统目录。
  - 顺便修好一个坑：跟奏窗口以前靠 exe 名字里有没有 `follow` 决定露不露，
    装出来的 `AutoPlay.exe` 会**没有跟奏**；现在 `FOLLOW_MODE` 一律打开。
- 图标改成**矢量画**的（`noteicon.py`）：以前拿 `QFont` 画「♪」，在没有字体的环境里
  （比如打包机上跑 offscreen）会画成一个空方块。现在主程序 / 安装程序 / 快捷方式共用同一个，
  顺手做了 `AutoPlay.ico`（7 个尺寸）打到 exe 上。

## 2026-09-20 v1.5

- **新增「简谱编辑器」`editor.py`**（打包成 `EditorPlay\AutoPlayEditor\AutoPlayEditor.exe`）：
  把音轨铺成钢琴卷帘，转出来的谱哪儿不对就手动改哪儿。
  - **看**：横轴时间、纵轴音高，方块长短 = 时值，方块上写着简谱记号（`E2` / `#A1` / `B5`…），
    颜色沿用跟奏窗口那套「按要按住的鼠标键分色」；上面有时间尺，左边是键盘，
    整八度那条线会加亮，黑键行压暗。
  - **改**：双击空白加音、右键音块删音、按住拖动挪位置 / 挪音高、拖右边缘改长短、
    方向键微调（Shift + 左右改时值、Shift + 上下跳八度）、`Del` 删选中、`Ctrl+Z` 撤销
    （撤销栈 200 步，一次拖动只算一步）。有「吸附网格」按网格对齐，「新音时长」定新音多长。
  - **听**：整首实时试听，下方进度条拖着就能跳（`winsound` 不能跳转，还是「切一段另存
    再放」那套）；**改完自动重新合成**并从当前光标接着放，改一个音听一个音；
    双击音块单独听这一个音。合成都放后台线程，界面不卡。
  - **存**：`.mproj` 工程文件（json，记音符 + 主音 + 速度 + 来源），没改完也能存、
    下次接着改；「导出 MIDI」写出**单音** `.mid`，压到后一个音上的尾巴会先剪短并记进日志。
  - 这个窗口不往游戏里发按键，所以 spec 里**没有** `uac_admin`，也不需要
    `keyboard` / `pydirectinput` / basic-pitch 那一套，成品约 100 MB。
  - 新目录 `EditorPlay\AutoPlayEditor\`（`build_editor.bat` 打包），跟 `dist\AutoPlay`、
    `FollowPlay\AutoPlayFollow` 三个版本互不覆盖。
- **修好中文音轨名乱码**：`midi_analyze.decode_text()` 原来只按 gbk 还原，遇到
  utf-8 存的轨道名会解不出来、直接显示成 `ä¸é³è¨åæ¯` 这种。现在按
  **utf-8 → gbk → 原样** 依次试，音轨下拉框里能看到「中音萨克斯」了。
- `preview.py` 里把「音高直接给 midi 号」的合成拆成 `synth_pitches()` / `Preview.render_pitches()`
  给编辑器用，原来的 `synth()` / `render()`（走简谱记号）变成薄薄一层包装，
  波形与从前逐样本一致 —— 主程序的试听路径没变。
- 版本号进界面：主程序 / 跟奏版 `v1.5`，编辑器自己从 `v1.0` 起步。

## 2026-09-20 v1.4

- **主旋律改成「融合」出一条**：以前转完会给两条候选（`melody2 high` = 每个时刻最高的那条，
  默认弹的那条 = 每个时刻最响的那条），用户反馈单听哪条都不理想。现在默认那条是两条规则
  **合起来**的结果：先比响度分档，同一档里再比音高 —— 明显更响的那个赢（伴奏抢不走旋律），
  响度差不多的听音高的（旋律一般在上方）。
  - 起点仍然是原始的多声部转写，每个时刻只留一个音，所以**依然是严格单音、绝不重叠**。
  - 实测那首和田薫：`最响` 248 个音 / 最长音 5.8 秒，`最高` 365 个音 / 最长音 **22.1 秒**
    （模型冒出来的假高音），融合后 283 个音 / 最长音 7.7 秒，两个毛病都躲开了。
  - 两条原始候选还写在 `.mid` 里（`melody2 loud` / `melody3 high`，跟融合结果一样时不写），
    听不惯可以继续在主程序里换音轨 A/B。
- **试听加了进度条**：在「♪ 试听」旁边，点一下或拖着就能跳到想听的地方，右边显示
  「0:12 / 1:35」。`winsound` 本身不能跳转，做法是把后面那一截另存成一个临时 wav 再放
  （切出来的开头补了 8 毫秒渐入，不会有咔哒声），上一个切片随后删掉。
  - 进度条是自绘点击位置的（普通 QSlider 点空白只会翻页）；手指按着的时候自动刷新会
    让位给手指，松手才真的跳。
  - 开始跟奏练习时也会先把试听停掉（原来只有「演奏」会）。
- 版本号进界面：`v1.4`。

## 2026-09-20 v1.3.1

- **修好打包版「音频转 MIDI」用不了 basic-pitch 的问题**。scipy 的私有扩展
  `_cyutility` 是 C 层隐式引用的（`scipy.sparse` 的 Cython 代码里 import 它），
  PyInstaller 的静态分析看不见，于是冻结后 `import scipy.stats` 直接
  `ModuleNotFoundError` —— 而 basic-pitch 正好要经 `mir_eval -> scipy.stats` 走进来。
  两个 spec 现在都手工把它带上；打包后的 exe 实测能在和田薫那首上跑完整条链
  （1513 个音事件）。
- 「后端探测」探深一层：以前只 import `basic_pitch` 顶层包就算「可以用」，现在真去
  import `basic_pitch.inference`，不会再出现「日志说三个后端都能用、一转就报没装」。
- 导入失败时把**真实异常**写进日志（原来只有笼统的「没有装 basic-pitch」），
  以后打包出问题一眼看得出缺了什么。

## 2026-09-20 v1.3

- **mp3 -> midi 换成正经模型**：不再用自己写的「先砍频段再猜基频」，改成调用 Spotify 开源的
  **basic-pitch**（`pip install basic-pitch`，跑自带的 ONNX 模型、不用装 TensorFlow）。
  它本来就是拿「带伴奏的完整编曲」训练的，独奏、有伴奏的歌都能转；转出来是**多声部**，
  我们再从里面挑一条**单音主旋律**（每个时刻留下最响的那个音），不再把音高限制在某个频段里乱截。
  没装 basic-pitch 时才退回 librosa.pyin / 内置 YIN，界面上会写明这次用的是哪个后端。
- 转出来的 midi 最多三条音轨：`melody lead`（单音主旋律，默认弹这条）、
  `melody2 high`（按「最高音」挑的另一条，跟默认不一样时才写）、
  `full all-notes`（完整转写，多音同时，留着核对或者丢进 DAW）。
- **跟奏窗口的颜色改成「按操作分」**：蓝 = 什么都不按、青 = 中键（升半音）、
  琥珀 = 右键（升调）、橙红 = 右键 + 中键、紫 = 左键（降调）、粉 = 左键 + 中键；
  窗口最上面加了一条图例，把颜色和操作一一对上 —— 等长演奏时色块太短写不下 `↑` / `#`
  记号，颜色就是唯一的线索；音够长时色块上照旧写记号。
- **「音长」默认改成「原样演奏」**：跟着 midi 的时值走，长短跟原曲一样。
- **彻底删掉「开机自动启动」**：界面、托盘菜单、设置项全没了；老版本往
  `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 里写过的自启项会在启动时自动清掉。

## 2026-09-20 v1.2

- 新增「**♪ 试听**」（`preview.py`）：按当前谱面自己合成一小段音频放出来，**转完音频先
  听一遍**，确认主旋律抓对了再进游戏自动演奏，省得白弹一遍。游戏内浮层里也能按。
  - 音色不用系统 MIDI 音源（不是每台机器都有、行为也不可控），而是拿 numpy 叠几个正弦
    当「琴音」（基音 + 三个泛音 + 指数衰减包络 + 起音 / 收尾渐入渐出），像音乐盒 / 电子琴，
    认音高足够；
  - 时间轴严格按谱面「音:持续时间」（**原样时值**），不受「音长 / 速度」影响；音尾照样
    裁到下一个音之前，所以试听也是单音；
  - 合成在后台线程（4 分钟的歌约 0.1 秒），播放交给 Windows 自带的 `winsound` 异步播放，
    放起来之后右上角状态胶囊写「试听 0:12 / 1:35」，放完自动收尾；
  - 再按一次「■ 停止」就停；**开始演奏 / 换文件 / 退出程序都会自动停**，不会和演奏同时响；
  - 临时文件放 `%TEMP%\AutoPlayPreview\`，每次试听前清掉上一次的；
  - 音频转完会在日志里提示「按『♪ 试听』听听主旋律抓对没有」。
- 新增 `jianpu.token_to_rel()` / `token_to_pitch()`：记号 -> 相对 tonic 的半音数 / midi 音高
  （`pitch_to_token` 的反操作），试听和以后别的地方算音高都用它。
- 版本号进界面：标题栏、副标题、托盘提示都带 `v1.2`。

## 2026-09-18 v1.1

- 新增「**音频转 MIDI…**」：可以直接选 mp3 / wav / flac，程序先把它转成**单音 MIDI**
  （只留主旋律、同时只响一个音），再走原来的选音轨 / 算 tonic / 生成简谱 / 自动演奏流程。
  游戏浮层里也能用：文件列表会同时列出 MIDI 和音频（音频是蓝色、带「（音频）」），
  双击音频就开始转。
- 转换算法做了三档，`auto` 会挑最好的那个：
  - **内置 YIN**（默认兜底，只用 numpy，不装任何东西）—— 独奏、清唱、单声部旋律；
  - **librosa.pyin**（`pip install librosa`）—— 单声部更稳，脏一点的录音也行；
  - **basic-pitch**（`pip install basic-pitch`）—— 多声部转写，每帧取最高声部当旋律，
    有伴奏的编曲也能转。

  用的哪个后端、识别出多少个音都会写在日志里；更重的 Melodia / Demucs 方案和详细
  对比见 `mp3midi/README.md`。
- 转换在后台线程里跑，界面不会卡；转完自动读进主界面（生成的 .mid 默认放在音频旁边）。
- v1.0（加音频转换之前那一版）原样保留在 `v1.0备份\` 里，想退回直接用里面的 exe。

## 2026-09-18 v1.0

- **`Ctrl+F1` 变成开关**：没露头就唤出来，已经贴在游戏上就再按一下收回去（只是藏起来，
  程序照常跑、热键照常管用；要退出还是走托盘右键菜单）。以前再按一下没反应，得挪鼠标
  去点右上角的 `×`。
- 新增「**凑不够就整首放慢**」（默认**不勾**）：勾上才把整首歌等比放慢来凑够「单音时长」，
  不勾就保持原速（挤在一起的音自己缩短）。上一版把这套放慢逻辑整个删了，这版给它做成
  开关 —— 想每个音都按满就勾上，介意曲子变慢就别勾。倍数会写在日志里（最多 8 倍）。
- 「最短按键」的提示跟着改：想每个音都按满，勾「凑不够就整首放慢」。
- 版本号进界面：标题栏、副标题、托盘提示都带 `v1.0`。

## 2026-09-18 v0.3.8

- **去掉「整首歌等比放慢」**：那是上一版为了让每个音都凑够「单音时长」而把整首歌
  拖慢的补丁，听着比原曲慢、不和谐。现在**时间轴一点不动** —— 等长演奏只改「每个音
  按住多久」，下一个音来得太早时这个音自己缩短，节奏该多快还是多快
  （`player.Player.shape` 不再返回放慢倍数，`stretch_factor` / `apply_factor` 已删除）。
- 「最短按键」退回成纯粹的「按下兜底」，「单音时长」也不再当整首歌的底线。
- 选文件分两个入口：**在桌面上照旧用系统文件框**（能打字、能粘路径，方便），只有按
  `Ctrl+F1` 从游戏里唤起主界面时才用浮层里的内嵌文件列表。靠 `show_window(in_game=True)`
  记的标记区分：托盘图标 / 启动时算桌面操作，`Ctrl+F1` 算游戏内。

## 2026-09-18 v0.3.7

- 选文件改成**嵌在主界面里的文件列表**（跟跟奏面板一个路子）：覆盖模式下点
  「选择 MIDI 文件…」不再新建任何窗口，游戏一点都不被打扰。上一版虽然让文件框
  自己置顶、不激活，但它内部的下拉列表（`QComboBox`）本来就是独立弹窗，
  还是可能把全屏游戏惊回桌面 —— 现在干脆一个窗都不开。
- 新的文件列表全是普通控件：`上一级 / 桌面 / 刷新` 三个按钮 + 当前路径，
  下面先列子文件夹（带 `/`）、再列 MIDI 文件；**双击**文件夹进入、**双击**文件开始读，
  也可以选中后点「打开」。打开时主界面自动撑高一点，关掉还原。
- 它跟浮层一样收不到键盘，**只用鼠标**操作；想打字（粘贴路径之类）就取消
  「游戏内覆盖」，那边照旧是系统文件框。

## 2026-09-18 v0.3.6

- 选文件的窗口不再把游戏顶掉：覆盖模式下「选择 MIDI 文件…」弹出的是程序自带的
  文件框，和主界面一样**置顶但不激活**（系统那个文件框一弹出来就抢前台，
  全屏游戏当场被打回桌面）。代价是这个文件框收不到键盘，**用鼠标双击选文件**，
  想打字就取消「游戏内覆盖」（那边照旧用系统文件框）。
- 覆盖面再扩一点：覆盖模式下主界面加了 `WS_EX_NOACTIVATE` —— 连点浮层里的按钮
  都不会把前台从游戏那儿抢走（代价是浮层收不到键盘，所以留了鼠标能点的 `✕`）。
  每次露头（`showEvent`）都会补一次，免得被 Qt 的显示流程重写掉。

## 2026-09-18 v0.3.5

- 新增「**音长**」，专治最短的音还是被游戏吞掉：
  - **等长演奏**（默认）：每个音都按「单音时长」（默认 90 毫秒）按住，长短一样，
    绝对不会吞音；
  - **原样演奏**：按 midi 的时值来，长短不一、更有起伏，但短音可能被吞。
  - 「单音时长」是底线：谱面里比它短的音会让**整首歌等比放慢**（等于降 BPM），
    长短比例不变 —— 只拉长那一个音会把节奏搞乱。放慢倍数写在日志里，最多 8 倍。
  - 演奏和跟奏（含练习模式的长条长度）用的是同一份整理过的谱面。
- 新增「**游戏内覆盖**」（默认开）：主界面去掉标题栏，`Ctrl+F1` 唤起时像录屏软件的
  浮层那样盖在游戏上 —— 置顶显示但**不抢游戏焦点**，独占全屏的游戏不会被打回桌面；
  顶部空白处按住可以拖动，`Esc` / 右上角 `×` 收起回游戏（退出改在托盘右键菜单）。
  关掉这个选项就回到原来的行为（带标题栏、抢前台、`Esc` 退出）。

## 2026-09-18 v0.3.4

- 新增**快捷键自定义**：界面上的「快捷键设置…」（托盘右键菜单里也有）里，
  点某一行的按键再按下新的组合键就改好了 —— 立刻生效、不用重启，键位记在设置里。
  原来那套 F6 / F7 / F8 / Ctrl+F1 / Ctrl+F2 一个都没动，改过的才生效
- 能绑的键扩大到字母、数字、F1–F24、小键盘、方向键、空格、`Esc`/`Tab`/`Enter`/标点
  （`hotkeys.VK_NAMES` 那张表）；组合键支持 Ctrl / Shift / Alt
- 重复的组合键会拦住并提示被哪个动作占了；每行可以单独恢复默认，也能一键全部恢复
- 界面底部的键位提示、托盘菜单里的 `(F6)` 这些字样，都会跟着当前键位变

## 2026-09-18 v0.3.3

- 跟奏面板的位置不再贴边：上下按屏幕高度留 7%（最少 40 像素）的空白，
  新的位置选项是 **底部偏上 / 屏幕正中 / 顶部偏下**，另外保留右上角、底部靠左、底部靠右
- 默认位置改成「底部偏上」；老版本存过的「底部居中」会自动映射过来，不用重新选

## 2026-09-18 v0.3.2

- 修「游戏里按 Ctrl+F1 唤不出主界面」：热键其实一直有触发（日志里能看到「唤起主窗口」），
  是窗口没顶到游戏前面 —— Qt 的 `raise_()` / `activateWindow()` 走 SetForegroundWindow，
  而 Windows 默认拒绝后台进程抢前台。现在改成：
  `SetWindowPos(HWND_TOPMOST)` 硬置顶 + `ShowWindow(SW_RESTORE)` 恢复 +
  `AttachThreadInput` 之后再抢前台，12 秒没在用就自动取消置顶（别一直压着游戏），
  开始演奏时也会先撤掉置顶再下沉
- 日志会多记一行「主窗口已顶到最前 / 没抢到前台焦点」，方便下次定位
- 唤起后的 12 秒里如果正开着「选择 midi」的文件对话框，就不撤置顶了，
  免得对话框连同主窗口一起沉到游戏后面
- 打包流程改了：跟奏版先打到暂存目录 `FollowPlayNew`，再用新增的 `应用跟奏版更新.bat`
  覆盖到 `FollowPlay`（脚本会等程序退出）。以前程序开着直接打包会把 `FollowPlay` 清空后写不进去，
  旧版本当场变成残缺的

## 2026-09-18 v0.3.1

- 跟奏新增「**跟奏节奏**」，默认是「**等我按对**」：程序一个键都不发，
  音符落到判定线就停住等你，按对那个键才继续往下走 —— 新手不用被原曲速度拖着跑；
  按对闪绿、按错闪红且不往前走，音符停在线上等的时候会描白边、顶上写「该你按了」。
  另一个选项「原速跟奏」就是原来那样跟着演奏器走
- 练习模式下 F7 暂停 / 继续、F8 停止照常，进度条和右上角浮窗跟着「按对了几个音」走
- 色块上直接写升降调 / 升半音的记号（`↑` 升调、`↓` 降调、`#` 升半音，组合 `#↑` / `#↓`），
  加了深色描边保证在深浅两种色块上都看得清；顶部那行也加了记号说明
- 音符从顶部状态行下面开始画，不再压到文字上

## 2026-09-18 v0.3.0

- 新增**跟奏模式**（`follow.py`）：无边框、半透明、磨砂质感的窗口盖在游戏上 ——
  长条音符按 midi 的时值下落，长条长度就是按住时长；落到圆角判定框就消失，
  超出框的部分不画；颜色按修饰键区分（正常蓝 / 升半音青 / 升调琥珀 /
  升调+升半音橙红 / 降调紫 / 降调+升半音粉）；哪个键被按住，那一列发光、
  判定线亮一条、琴键跟着高亮
- 跟奏窗口鼠标穿透、不抢焦点，时间轴和演奏器共用一个钟，暂停时音符一起停住
- 新增 `Ctrl+F2` 开关跟奏；界面上有「跟奏模式」勾选框和「跟奏位置」下拉（4 个位置，会记住）
- 新增含跟奏版的独立产物 `FollowPlay\AutoPlayFollow\AutoPlayFollow.exe`，
  以及 `AutoPlayFollow.spec` / `build_follow.bat`；`AutoPlay.exe` 保持干净的老版本
  （靠 exe 名字里有没有 follow 区分）
- 修「最短按键」数字框白底白字：补了 `QSpinBox` / `QCheckBox` 样式，
  并给整个程序换上深色调色板（`dark_palette()`）
- `player.py` 新增 `elapsed()`：对外暴露「演奏到第几秒」，暂停时冻住
- README 补跟奏模式说明和界面截图（`docs/`）

## 2026-09-18 v0.2.0

- 新增「开机自动启动」：默认打开，写 `HKCU\...\CurrentVersion\Run`，界面和托盘菜单都能关
- 新增「最短按键」调节（10~200 毫秒，默认 35）：游戏按帧读键盘，按太短会吞音，
  短于这个值的音会被拉长到它，长音仍按 midi 的时值走
- 演奏改成**按住式**：按满 midi 里的真实时值才松开，不再是每个音一样长的点按
- 新增 `Ctrl+F1` 在游戏里唤起主界面；`ESC` 改成只在主窗口里有效（游戏里按不会退）
- 右上角浮窗在没有演奏但已经读好 midi 时显示「已就绪 + 0/总音数」
- 修「自动闪退」：退出改走 `os._exit()`，跳过 Qt 析构（崩溃落在 `pyside6` 里就是这儿）；
  托盘菜单自己留引用，免得被 Python 回收后一点就崩
- 崩溃 / 出错会记到 `%LOCALAPPDATA%\AutoPlay\AutoPlay.log`，另加 `faulthandler` 留调用栈
- 多音轨自动挑主旋律（轨道名优先，其次音符最多），界面音轨下拉框可手动改选，
  手动选的谱面文件名带 `#t序号` 后缀

## 2026-09-18 v0.1.0

- 初版：读 midi -> 推断最干净 tonic -> 生成简谱 txt -> 在游戏里自动按键演奏
- PySide6 界面、系统托盘、全局热键（F6 / F7 / F8）、右上角半透明进度浮窗
