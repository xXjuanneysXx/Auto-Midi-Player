# 发布说明（给自己看的，发仓库之前扫一眼）

这个文件夹就是**准备推到 GitHub 的那一份源码**，从开发目录里挑出来的：

* 全部 `.py` / `.spec` / `.bat` / 文档 / `docs\` / `mp3midi\`，跟开发目录一致；
* `曲库索引\`（`library.json` + 说明）；
* **没有**构建产物（`build\` `dist\` `发布\` `EditorPlay\` `FollowPlay\` `v1.0备份\`
  `__pycache__\` `TONIC*.txt`）—— `.gitignore` 里都排除了；
* **没有**那三个测试用的 midi（`1.mid`、`Going Home.mid`、`ソレソレ (来吧来吧).mid`）：
  都是有版权的曲子，公开仓库里放着容易被投诉下架。

## 令牌

`github_token_local.py` 里换成了占位符：

```python
TOKEN = 'GITHUB_PERSONAL_TOKEN'
```

程序把占位符当成「没内置令牌」：上传时会提示一句，**下载曲库 / 演奏 / 编辑器照常**。
自己要用上传功能就把真令牌填进去，但**别提交**（`.gitignore` 里有说明）。
想让别人也能上传：让他自己填一份；或者你另打一个带自己令牌的包 ——
带令牌的包别公开传。

## 怎么跑 / 怎么打包

* 源码跑：Python 3.12 + `pip install pyside6 numpy mido`，然后 `python main.py`；
* 完全版还要 `basic-pitch`（音频转 MIDI），精简版不需要；
* 打包：`build_installer.bat`（等价于 `python -u -X utf8 make_installer.py all`），
  出来的两个安装程序在 `发布\` 里。

## 安装包放哪儿

**别提交进仓库**（`发布\` 已经在 `.gitignore` 里了，200 MB 的东西也不该进 git）。
发 GitHub Release，把安装包拖进 `Attach binaries` 就行。

## 版权

`songs\鸟之诗.mid` 是程序启动默认载入的那首，**它也是有版权的曲子**。
留着它，程序开箱就有东西可放；不想担这个风险就删掉 ——
程序照样能跑，只是启动时曲库是空的，用户得自己「选择 MIDI 文件…」。


## 上传中转：让用户能传曲子，而仓库里不带令牌

这份源码里，`github_token_local.py` 只有一个占位符，
`relay_source.py` 的 `RELAY_URL` 也是空的 —— 上传走哪条路，由你自己定：

* 想让用户不用填令牌就能传，**且不想把令牌放进安装包**：
  照 `中转上传\部署说明.md` 搭一个 Cloudflare Worker（免费），然后

  ```text
  python -X utf8 set_relay_url.py https://你的地址.workers.dev --strip-token
  ```

  `--strip-token` 会把内置令牌清掉。这样打出来的安装包可以直接公开发布。
* 想继续用内置令牌（老做法）：把令牌填回 `github_token_local.py`。
  ⚠ 那样打出来的包**不能公开**：拀出来就能删你曲库里的文件，
  GitHub 还会自动把暴露的令牌作废。
